export interface AddPostMenuItem {
  type: string;
  icon: string;
  color: string;
}

export interface BottomMenuItem {
  name: string;
  path: string;
  title: string;
  icon: string;
  color: string;
}

export interface SidebarMenuItem {
  title: string;
  icon: string;
  link: string;
}
